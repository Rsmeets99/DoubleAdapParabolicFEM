# Python helper and plotting dependencies for local development.
# The interpreter version is determined by the Python executable used to create
# .venv. On the current Ubuntu setup, `python3 -m venv .venv` creates a Python
# 3.12 environment, so keep dependency floors compatible with Python 3.12.
#
# Interactive Matplotlib windows on Ubuntu/WSL also need the system Tk package:
#   sudo apt install python3-tk
numpy>=1.26,<3
matplotlib>=3.8,<4
